# Gradle
.gradle/
build/
app/build/
gradle-app.setting
!gradle/wrapper/gradle-wrapper.jar
!gradle/wrapper/gradle-wrapper.properties

# Local config
local.properties

# Release signing key - never commit
keystore/

# R8 mapping.txt per versionCode (see CLAUDE.md's release flow) - per-build output, not source
mapping/

# IDE
.idea/
*.iml
.DS_Store

# Build outputs
*.apk
*.aab
*.ap_
*.dex

# Kotlin incremental
.kotlin/

# Logs / temp
build_out.txt

# Emulator harness (scripts/emu.sh) scratch output — scripts/baseline/ IS committed (golden files)
scripts/shots/
scripts/.testlib/

# Staged release APKs (copied out of app/build for upload)
dist/
# Icon contact sheets generated while designing launcher icons
icon_sheet.png
